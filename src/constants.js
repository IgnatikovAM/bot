export const serviceTypes = new Set([
  'notification','notification_template','call_log',
  'revoked','e2e_notification','protocol'
]);

export const allowedTypes = new Set(['text','voice','audio','music']);
